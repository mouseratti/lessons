[package]
name = "lc_9_palindrome_rust"
version = "0.1.0"
edition = "2024"

[dependencies]
criterion = { version = "0.7.0", features = ["html_reports"] }


[[bench]]
name = "is_palindrome_bench"
harness = false